import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  tabBarContainer: {
    width: '100%',
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignItems: 'center',
  },
});
